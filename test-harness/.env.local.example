# Copy this file to .env.local and fill in your keys.
# .env.local is gitignored and will never be committed.

# OpenAI API key — enables:
#   - Lightweight BigBoss routing (gpt-4o-mini, ~2s, ~$0.001 per call)
#   - Intent parsing for voice commands
# Without this key, BigBoss falls back to the full Cursor agent CLI (~9s).
OPENAI_API_KEY=

# Override the default port (3000)
# PORT=3000

# Override the skills directory
# SKILLS_ROOT=

# Override the Cursor agent CLI path
# CURSOR_CLI=

# Cursor Agent model (default: auto — avoids Opus usage limits; set to opus-4.6 etc. if desired)
# CURSOR_AGENT_MODEL=auto
